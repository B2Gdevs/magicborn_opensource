# Use legacy peer dependency resolution to avoid warnings from Payload CMS
# Payload has internal @dnd-kit dependencies that we don't use directly
# This allows npm to be more lenient with peer dependency mismatches
legacy-peer-deps=true

