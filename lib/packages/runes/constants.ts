// lib/packages/runes/constants.ts
// Rune constants and convenience aliases

import type { RuneCode } from "@core/types";

/**
 * Convenience aliases so we don't scatter raw "F", "A", etc. literals.
 */
export const RC = {
  Air: "A" as RuneCode,
  Burst: "B" as RuneCode,
  Crystal: "C" as RuneCode,
  Duration: "D" as RuneCode,
  Energy: "E" as RuneCode,
  Fire: "F" as RuneCode,
  Gravity: "G" as RuneCode,
  Heal: "H" as RuneCode,
  Ice: "I" as RuneCode,
  Jolt: "J" as RuneCode,
  Kinetic: "K" as RuneCode,
  Light: "L" as RuneCode,
  Mind: "M" as RuneCode,
  Null: "N" as RuneCode,
  Order: "O" as RuneCode,
  Persuasion: "P" as RuneCode,
  Quake: "Q" as RuneCode,
  Ray: "R" as RuneCode,
  Self: "S" as RuneCode,
  Target: "T" as RuneCode,
  Unbind: "U" as RuneCode,
  Void: "V" as RuneCode,
  Water: "W" as RuneCode,
  Amplify: "X" as RuneCode,
  Yield: "Y" as RuneCode,
  Zeal: "Z" as RuneCode,
};

